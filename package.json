{
  "name": "jsonrpc-http-server",
  "description": "Server running jsonrpc-http for a given API",
  "version": "0.2.1",
  "main": "./lib/server.js",
  "dependencies": {
    "rester-tools": ">= 0.6.3 < 0.7",
    "introspect": ">= 0.0.2 < 0.1",
    "express": ">= 3.3.4 < 4"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/jakobmattsson/jsonrpc-http-server.git"
  },
  "devDependencies": {
    "underscore": ">= 1.4.3 < 2",
    "should": ">= 1.2.1 < 2",
    "jscov": ">= 0.5.4 < 0.6",
    "mocha-term-cov-reporter": ">= 0.1.0 < 0.2",
    "bufferstream": ">= 0.5.1 < 0.6",
    "coffee-script": ">= 1.6.0 < 2",
    "mocha": ">= 1.7.4 < 2",
    "request": ">= 2.12.0 < 3"
  },
  "engines": {
    "node": ">= 0.8 < 0.12"
  },
  "scripts": {
    "pretest": "coffee -co lib src",
    "prepublish": "coffee -co lib src",
    "start": "bin/locke",
    "coverage": "jscov src .cov && SRC_DIR=.cov mocha --compilers coffee:coffee-script --reporter mocha-term-cov-reporter tests/*.coffee",
    "test": "mocha --compilers coffee:coffee-script tests/*.coffee"
  }
}
